# Polish round 2 acceptance map

Review candidate: `3bcd3e50d23963398d0f416f3e7450a4b79fc1dc`

Review report: `afcd0f2c57d82224128695cc27d229a865f61084`

Repair commit: `16aff877e2b476fe9278cdde31638cb952aa791a`

Release: `v0.1.18`

Live URL: <https://presence-bridge.sociobot.in>

Every finding in `.factory/review-1.md` and `.factory/review-2.md` was treated as acceptance work and rechecked after deployment.

| Finding | Change made | Automated evidence | Visual and live evidence |
| --- | --- | --- | --- |
| F-1-1 | Demo state remains session-only. Reset, Start for real, Browser Back, and closing/reopening all discard edits and recreate the seed. | `@claim:demo-exit-discard`; `@claim:demo-isolation`. | `live-browser-check.json` records null demo and real keys after exit and `available` after Back. |
| F-1-2 | No unproved `$24` claim or live checkout remains. Purchase surfaces explain that Bridge Plus is unavailable and keep the free roster usable. | `@claim:checkout-availability`; claims contract unit test. | Live route sweep and 88-test live suite passed. |
| F-1-3 | The compact mobile header keeps Privacy visible at 390 px. | `390px first screen keeps Privacy and all three facts visible`. | `live-mobile-first-screen.png`; live width is 390/390 with no overflow. |
| F-1-4 | Phone hero spacing keeps all three facts in the first 844 px. | `390px first screen keeps Privacy and all three facts visible`. | `live-browser-check.json` records fact bottoms 618.80, 643.13, and 667.45 px. |
| F-1-5 | `/app.html` retains its own title, description, canonical, social metadata, icons, manifest, legal navigation, footer, and sitemap entry. | `the real roster route has complete metadata and legal navigation`; `accessible page /app.html`. | `live-verify-app/verify.json`; live title is `Presence Bridge — Your local team roster`. |
| F-1-6 | Every site route updates title, description, canonical and social metadata; History navigation focuses the h1; unknown paths return the styled HTTP 404. | `site routes update metadata, history, and heading focus`; `unknown paths return the styled 404 on navigation and refresh`. | `live-browser-check.json` records all route titles/canonicals and 404 status/title/return link. |
| F-1-7 | Product copy consistently uses “contact tool”; headings and actions name the task rather than brand lore. | `the committed copy audit matches rendered copy and README`. | `desktop-home.png`; the generated `.factory/copy-audit.md` has no overlong or banned copy. |
| F-1-8 | README privacy statements remain split into short, single-purpose sentences. | Copy-audit drift test and generated word counts. | Published repository README and `.factory/copy-audit.md`. |
| F-2-1 | The landing action now promises “four sample teammates and your status.” On phones the duplicate demo introduction collapses, placing Ava and Leo in the first viewport while retaining the banner and current status. | `@claim:demo-seed-and-first-view` opens `/`, clicks once, asserts four rows and measures the first two rows at 390 × 844. | `live-mobile-demo-first-view.png`; live row bottoms are 568.69 and 666.66 px. |
| F-2-2 | Expanded the registry to 24 claims. The demo test now covers Reset, exit, and close/reopen. Added exact platform, signing, and no-payment-runtime claims and tests; signing copy is conditional rather than absolute. | `@claim:demo-exit-discard`, `@claim:release-platforms`, `@claim:signing-configuration`, `@claim:no-payment-runtime`; unit test enforces exactly one tagged test per claim. | `clean-claims.json` records 24/24 exact commands passing from a clean clone. |
| F-2-3 | Replaced the hand-counted audit with a generator that extracts rendered landing copy, alt text, accessible names, and README prose through one whitespace counter. | `npm run audit:copy`; `the committed copy audit matches rendered copy and README`. | `.factory/copy-audit.md` is regenerated and drift-protected. |
| F-2-4 | Visible controls now read “Open settings” and “Open {contact tool}”. | `@claim:contact-handoff`; accessibility route and touch-target tests. | `live-mobile-demo-first-view.png` shows the verb-led settings action; the selected contact action is generated by the same renderer. |
| F-2-5 | Added an opt-in desktop folder watcher. It grants a chosen folder, reads only bounded `.presence.json` files, refreshes newer updates every five seconds, shows update times, marks day-old entries stale, and can stop and remove the grant. Demo and web builds cannot grant native folders. | `@claim:shared-folder-refresh`; Rust `reads_only_bounded_presence_files`; TypeScript/Rust full suites. | Settings exposes “Watch a shared folder,” “Check now,” and “Stop watching” only in the installed desktop environment. |

## End-to-end evidence

- Clean clone `/tmp/presence-bridge-claims-FAOUNW` at the repair commit: 24/24 exact claim commands passed. Evidence: `clean-claims.json`.
- Clean-clone full gate: 14 Vitest tests and 88 Playwright tests passed; two desktop-project skips are intentionally mobile-only. TypeScript lint, production build, copy audit, and dependency audit passed.
- Rust: formatting, locked check, and locked test passed; the native file-filter test passed.
- Live origin: the same 88 Playwright tests passed with two intentional skips.
- Accessibility: Playwright Axe found zero serious or critical violations across `/`, `/demo`, `/privacy`, `/terms`, `/download`, `/app.html`, and the 404 route. `verify-url.sh` passed home, demo, and app with no console, structure, alt, or button-name errors.
- Privacy and offline: the live landing-to-demo flow made only same-origin requests; no real roster key was created. A new context reloaded the demo offline with Ava visible.
- Lighthouse on the cold live URL: Performance 100, Accessibility 100, Best Practices 100, SEO 100; LCP 1.21 s, CLS 0, TBT 18.5 ms, 149,378 transferred bytes.
- Screenshots: `live-mobile-first-screen.png`, `live-mobile-demo-first-view.png`, `mobile-first-screen.png`, `mobile-demo-first-view.png`, and `desktop-home.png`.
- Release and deployment: every release job passed; the published Debian package matched `SHA256SUMS`; live HTML, service worker, CSS, and JavaScript matched the tested build byte-for-byte.

The post-deploy cold check found no unresolved review finding.
